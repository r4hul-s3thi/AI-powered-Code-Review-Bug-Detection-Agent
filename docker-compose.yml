version: "3.9"

services:
  agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Hot-reload in dev: mount source so changes reflect without rebuild
      - .:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
